# live-ai
Terminal + browser AI assistant with live web search, tool calling, and persistent memory — built on Claude's native web search tool. Includes a self-verification pass that fact-checks its own answers against fresh sources before you trust them.
